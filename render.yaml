services:
  - type: web
    name: edubot-smart-assistant
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300 --preload"
    healthCheckPath: /
    envVars:
      - key: FLASK_ENV
        value: production
      - key: RENDER
        value: true
      - key: SECRET_KEY
        generateValue: true
      - key: SESSION_TYPE
        value: filesystem
    autoDeploy: true
